{
  "name": "nodec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript port of C11 compiler for JavaScript runtime",
  "license": "MIT",
  "bin": {
    "nodec": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "hello": "npm run build && node dist/cli.js run examples/hello.c",
    "bench": "npm run build && node scripts/bench-vs-js.mjs",
    "mjs:test": "npm run build && node dist/cli.js run examples/mjs_test_runner.c",
    "mjs:inception": "npm run build && node dist/cli.js run examples/mjs_inception_full.c"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.7.2"
  }
}
